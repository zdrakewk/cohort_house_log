<h1>This is the show page for <%= @room_obj.theme %> room theme.</h1>

<%#= @room_obj.attributes %>
<%# {"id"=>3, "name"=>"bathroom2", "theme"=>"Mermaids", "use"=>true}
%>

<p>The <%= @room_obj.name %> is currently <%= @room_obj.use ? "in use" : "not in use Enjoy" %>! </p>

<%# if @room_obj.use %>
<%# else %>
<%# end %>

<form action='/rooms/<%= @room_obj.id %>' method='post'>
  <input type='hidden' name='_method' value='delete'/>
  <label>Are you sure?!?!<label><br>
  <input type='submit' value='Delete'><br>
  <label>Sit here and think about your decisions  <label>
<form>